{
  "version": 2,
  "routes": [{ "src": "/(.*)", "dest": "app.js" }],
  "builds": [
    {
      "src": "app.js",
      "use": "@vercel/node"
    }
  ],
  "scripts": {
    "build": "npm run build",
    "install": "npm ci"
  }
}
